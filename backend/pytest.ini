[pytest]
# Add the backend directory itself to sys.path so tests can import modules directly
# (e.g. `from connectivity import ...` instead of `from backend.connectivity import ...`)
pythonpath = .
testpaths = tests

# Automatically treat all async test functions as asyncio coroutines
asyncio_mode = auto
