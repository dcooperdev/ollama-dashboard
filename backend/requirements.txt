# Core web framework and ASGI server
fastapi>=0.111.0
uvicorn[standard]>=0.29.0

# Async HTTP client — used for all Ollama API calls and connectivity checks
httpx>=0.27.0

# Testing stack
pytest>=8.2.0
pytest-asyncio>=0.23.0
